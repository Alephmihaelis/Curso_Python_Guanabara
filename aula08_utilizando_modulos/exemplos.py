
# import math → importa todas as funcionalidades de `math`

from math import sqrt, ceil, floor # → importa somente as funcionalidades `sqrt` e `ceil` de `math`

num = int(input('Número: '))
print('A raiz de {} vale {:.2f}'.format(num, sqrt(num))) # → Mais uma vez, estou evitando o uso de variáveis desnecessárias
