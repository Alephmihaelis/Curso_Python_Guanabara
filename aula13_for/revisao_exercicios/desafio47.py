
# `Loop` que percorre o intervalo de 1 a 51:
for par in range(1, 51):
    
    # Verifica se o número informado por `par` é par: 
    if par % 2 == 0:

        # Imprime na tela os números pares, e retira a quebra de linha:
        print(par, end=' ')
